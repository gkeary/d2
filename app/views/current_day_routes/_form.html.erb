<%= form_for(@current_day_route) do |f| %>
  <% if @current_day_route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_day_route.errors.count, "error") %> prohibited this current_day_route from being saved:</h2>

      <ul>
      <% @current_day_route.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
